explodio
========

Dev setup:
 - Install `kubectl`
 - Install `minikube`
 - Starting `minikube`:
    - `minikube start --cpus 4 --memory 4096`
 - Preparing postgres:
    - `cd postgres`
    - `./kube_install.sh`


